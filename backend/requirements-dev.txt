# GestureFlow backend - development dependencies (running tests and linting).
# These are NOT installed on the production server - they are only for local work and CI.
#
# Install everything (runtime + dev) for local development with:
#     pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
httpx==0.28.1
ruff==0.8.6
