# Screenshots (placeholder)

No screenshots have been captured yet — this environment has no browser
automation tooling available, so none were generated or faked.

To add real screenshots, run the app (locally or against the deployed URLs) and
save PNGs here with these exact names, then they will appear automatically in
the root `README.md` (already wired up with relative Markdown image links):

| File | Page | What to capture |
|---|---|---|
| `live.png` | `/` (Live) | Webcam active, hand skeleton overlay, a recognized gesture shown with its confidence |
| `dashboard.png` | `/dashboard` | The gesture-frequency chart, confidence-over-time chart, and history timeline with some session data in them |
| `model.png` | `/model` | The rule-vs-ML comparison table and confusion matrices |
| `about.png` | `/about` | The architecture / pipeline explanation page |

Suggested size: ~1280px wide, PNG. Crop out any personal browser chrome you'd
rather not publish.
